<div class="comment-form" style="margin: 0; padding: 24px; background: linear-gradient(135deg, rgba(16, 185, 129, 0.08) 0%, rgba(16, 185, 129, 0.03) 100%); border: 2px solid var(--border-color); border-radius: 12px;">
    <h3 style="font-size: 18px; font-weight: 700; margin-bottom: 16px; color: var(--text-dark); display: flex; align-items: center; gap: 8px;">
        ✍️ Ajouter un commentaire
    </h3>
    
    <style>
        .comment-textarea-group { margin-bottom: 16px; }
        .comment-textarea-group label { 
            display: block; 
            margin-bottom: 8px; 
            color: var(--text-dark); 
            font-weight: 600; 
            font-size: 14px; 
        }
        .comment-textarea-group input,
        .comment-textarea-group textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            font-size: 14px;
            font-family: inherit;
            box-sizing: border-box;
        }
        .comment-textarea-group textarea {
            min-height: 100px;
            resize: vertical;
        }
        .comment-textarea-group input::placeholder,
        .comment-textarea-group textarea::placeholder {
            color: #d1d5db;
        }
        .comment-textarea-group input:focus,
        .comment-textarea-group textarea:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.15);
            background: rgba(16, 185, 129, 0.01);
        }
        .comment-actions { display: flex; gap: 12px; align-items: center; }
        .comment-actions button { 
            padding: 12px 24px; 
            border: none; 
            border-radius: 8px; 
            font-weight: 600; 
            cursor: pointer; 
            background: var(--primary);
            color: white;
            font-size: 14px;
            transition: all 0.2s;
        }
        .comment-actions button:hover { 
            background: var(--primary-hover);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
        }
    </style>
    
    <form method="post" action="{{ path('app_commentaire_new', {'annonce_id': annonce.id}) }}">
        <div class="comment-textarea-group">
            <label for="auteur_anonyme">Votre nom {% if not app.user %}<span style="color: var(--text-muted);">(optionnel - laissez vide pour rester anonyme)</span>{% endif %}</label>
            {% if app.user %}
                <input type="text" id="auteur_anonyme" name="auteur_anonyme" value="{{ app.user.prenom }} {{ app.user.nom }}" readonly style="background: #f3f4f6;">
            {% else %}
                <input type="text" id="auteur_anonyme" name="auteur_anonyme" placeholder="Votre nom ou 'Anonyme'">
            {% endif %}
        </div>

        <div class="comment-textarea-group">
            <label for="contenu">Partagez votre avis sur cette annonce</label>
            <textarea 
                id="contenu"
                name="contenu" 
                required 
                placeholder="Écrivez votre commentaire ici... Soyez constructif et respectueux 😊"
                rows="4"></textarea>
        </div>

        <div class="comment-actions">
            <button type="submit">📤 Publier</button>
            <span style="color: var(--text-muted); font-size: 13px;">Les commentaires sont visibles immédiatement</span>
        </div>
    </form>
</div>



