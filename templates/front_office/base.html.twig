<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sport Insight Boutique{% endblock %}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@500;600;700;800&family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --pitch-900: #0b1b14;
            --pitch-800: #123023;
            --pitch-700: #1b4a35;
            --pitch-600: #1f6b49;
            --pitch-500: #2b8b60;
            --line: #e5e7eb;
            --surface: #f6f8f7;
            --ink: #101827;
            --muted: #5e6b66;
            --warn: #f59e0b;
            --danger: #ef4444;
            --ok: #22c55e;
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            font-family: 'Manrope', sans-serif;
            background: radial-gradient(circle at 0% 0%, #eef8f1 0, #f8faf9 44%, #f3f5f4 100%);
            color: var(--ink);
        }

        h1, h2, h3, h4, h5 {
            font-family: 'Barlow Condensed', sans-serif;
            letter-spacing: .3px;
        }

        .top-navbar {
            background: #111827;
            padding: 0 40px;
            height: 64px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .nav-brand {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
            color: #fff;
        }

        .nav-brand-icon {
            width: 34px;
            height: 34px;
            border-radius: 8px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #10b981;
            color: #fff;
            font-family: 'Barlow Condensed', sans-serif;
            font-weight: 700;
        }

        .nav-brand-text {
            display: block;
            color: #fff;
            font-size: 18px;
            font-weight: 700;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 4px;
            list-style: none;
            margin: 0;
            padding: 0;
        }

        .nav-links a {
            text-decoration: none;
            color: #9ca3af;
            font-size: 13px;
            font-weight: 700;
            padding: 8px 14px;
            border-radius: 6px;
        }

        .nav-links a.active {
            color: #10b981;
            background: rgba(16, 185, 129, 0.1);
        }

        .nav-links a:hover {
            color: #fff;
            background: rgba(255, 255, 255, 0.06);
        }

        .nav-actions {
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }

        .btn-chip {
            background: rgba(255,255,255,0.08);
            color: #fff;
            border: 1px solid rgba(255,255,255,0.12);
            border-radius: 6px;
            text-decoration: none;
            padding: 7px 12px;
            font-size: 13px;
            font-weight: 700;
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }

        .btn-chip--cta {
            background: rgba(16, 185, 129, 0.15);
            border: 1px solid rgba(16, 185, 129, 0.35);
        }

        .cart-pill {
            min-width: 22px;
            text-align: center;
            border-radius: 999px;
            background: #ef4444;
            color: #fff;
            font-size: 11px;
            padding: 2px 6px;
        }

        .hero {
            margin: 22px auto 0;
            max-width: 1320px;
            border-radius: 20px;
            overflow: hidden;
            background: linear-gradient(120deg, var(--pitch-900), var(--pitch-600));
            color: #fff;
            padding: 52px 34px;
        }

        .hero h1 {
            font-size: 58px;
            margin: 0;
            line-height: .95;
        }

        .hero p {
            font-size: 16px;
            color: #ddf7e9;
            margin-top: 12px;
            max-width: 700px;
        }

        .page-container {
            max-width: 1320px;
            margin: 24px auto;
            padding: 0 18px;
        }

        .section-title {
            font-size: 36px;
            margin: 0 0 16px;
        }

        .front-card {
            background: #fff;
            border: 1px solid var(--line);
            border-radius: 16px;
            padding: 18px;
            box-shadow: 0 10px 30px rgba(16, 24, 39, 0.04);
        }

        .front-card:hover {
            box-shadow: 0 16px 32px rgba(16, 24, 39, 0.08);
        }

        .badge-soft {
            display: inline-block;
            background: #eaf6ef;
            color: var(--pitch-700);
            border-radius: 999px;
            padding: 5px 10px;
            font-size: 12px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: .5px;
        }

        .stock-ok { color: #15803d; font-weight: 700; }
        .stock-low { color: #b45309; font-weight: 700; }
        .stock-out { color: #b91c1c; font-weight: 700; }

        .btn {
            border-radius: 10px !important;
            font-weight: 700 !important;
            padding: 10px 14px !important;
        }

        .btn-primary {
            border: 0 !important;
            background: linear-gradient(120deg, var(--pitch-700), var(--pitch-500)) !important;
        }

        .btn-secondary {
            border: 1px solid #cbd5e1 !important;
            color: #334155 !important;
            background: #fff !important;
        }

        .site-footer {
            margin-top: 54px;
            background: var(--pitch-900);
            color: #c2d5ca;
            padding: 26px 16px;
            text-align: center;
            font-size: 13px;
        }

        .flash-stack {
            margin-bottom: 16px;
            display: grid;
            gap: 10px;
        }

        .flash-item {
            border-radius: 10px;
            padding: 10px 12px;
            font-size: 14px;
            font-weight: 700;
        }

        .flash-success { background: #e8f9ee; color: #166534; border: 1px solid #bbf7d0; }
        .flash-warning { background: #fff7e5; color: #92400e; border: 1px solid #fde68a; }
        .flash-danger, .flash-error { background: #fef0f0; color: #991b1b; border: 1px solid #fecaca; }

        @media (max-width: 980px) {
            .top-navbar {
                padding: 0 16px;
            }

            .nav-links { display: none; }
            .nav-actions { gap: 8px; }
            .hero h1 { font-size: 42px; }
        }

        @media (max-width: 640px) {
            .hero {
                border-radius: 0;
                margin-top: 0;
                padding: 40px 20px;
            }

            .hero h1 { font-size: 35px; }
            .section-title { font-size: 28px; }
            .page-container { padding: 0 12px; }
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {% set cartCount = app.request.session.get('cart', {})|length %}

    <nav class="top-navbar">
        <a href="{{ path('front_home') }}" class="nav-brand">
            <span class="nav-brand-icon">SI</span>
            <span class="nav-brand-text">Sport Insight</span>
        </a>

        <ul class="nav-links">
            <li><a href="{{ path('front_entrainement_index') }}" class="{{ app.request.get('_route') starts with 'front_entrainement' ? 'active' : '' }}">Entrainement</a></li>
            <li><a href="{{ path('front_offre_index') }}" class="{{ app.request.get('_route') starts with 'front_offre' ? 'active' : '' }}">Offres</a></li>
            <li><a href="{{ path('front_match_index') }}" class="{{ app.request.get('_route') starts with 'front_match' ? 'active' : '' }}">Matchs</a></li>
            <li><a href="{{ path('front_sponsoring_index') }}" class="{{ app.request.get('_route') starts with 'front_sponsoring' ? 'active' : '' }}">Sponsoring</a></li>
            <li><a href="{{ path('front_equipement_index') }}" class="{{ app.request.get('_route') starts with 'front_equipement' ? 'active' : '' }}">Equipement</a></li>
        </ul>

        <div class="nav-actions">
            <a href="{{ path('front_equipement_orders') }}" class="btn-chip">Mon compte</a>
            <a href="{{ path('front_equipement_cart') }}" class="btn-chip btn-chip--cta">Panier <span class="cart-pill">{{ cartCount }}</span></a>
            {% if app.user %}
                <a href="{{ path('app_logout') }}" class="btn-chip">Deconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn-chip">Connexion</a>
            {% endif %}
            <a href="{{ path('back_entrainement_index') }}" class="btn-chip">Administration</a>
        </div>
    </nav>

    {{ render(path('trending_banner')) }}

    {% block hero %}{% endblock %}

    <div class="page-container">
        <div class="flash-stack">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="flash-item flash-{{ label }}">{{ message }}</div>
                {% endfor %}
            {% endfor %}
        </div>

        {% block body %}{% endblock %}
    </div>

    {% block extra_chat %}{% endblock %}

    <footer class="site-footer">
        <p>&copy; {{ "now"|date("Y") }} Sport Insight Boutique Football. Tous droits reserves.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    {% include '_partials/global_form_ux.html.twig' %}
    {% block javascripts %}{% endblock %}
</body>
</html>
