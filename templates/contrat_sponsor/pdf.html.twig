<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contrat Sponsor - {{ contrat_sponsor.sponsor.nom }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background: white;
            padding: 20px;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            border: 1px solid #ddd;
        }
        
        .header {
            text-align: center;
            margin-bottom: 40px;
            border-bottom: 3px solid #10b981;
            padding-bottom: 20px;
        }
        
        .header h1 {
            font-size: 28px;
            color: #10b981;
            margin-bottom: 5px;
        }
        
        .header p {
            color: #666;
            font-size: 14px;
        }
        
        .content {
            margin-bottom: 30px;
        }
        
        .section {
            margin-bottom: 25px;
        }
        
        .section-title {
            font-size: 16px;
            font-weight: bold;
            color: #111827;
            margin-bottom: 12px;
            border-left: 4px solid #10b981;
            padding-left: 10px;
        }
        
        .info-group {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 15px;
        }
        
        .info-group.full-width {
            grid-template-columns: 1fr;
        }
        
        .info-item {
            padding: 12px;
            background: #f9fafb;
            border-radius: 6px;
        }
        
        .info-label {
            font-size: 12px;
            color: #6b7280;
            text-transform: uppercase;
            font-weight: 600;
            margin-bottom: 5px;
        }
        
        .info-value {
            font-size: 16px;
            color: #111827;
            font-weight: 500;
        }
        
        .full-width {
            grid-column: 1 / -1;
        }
        
        .signature-section {
            margin-top: 50px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
        }
        
        .signature-box {
            text-align: center;
        }
        
        .signature-line {
            border-top: 1px solid #333;
            margin-top: 50px;
            padding-top: 10px;
            font-size: 12px;
            color: #666;
        }
        
        .footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 12px;
        }
        
        @media print {
            body {
                padding: 0;
            }
            .container {
                border: none;
                padding: 0;
            }
            .print-button {
                display: none;
            }
        }
        
        .print-button {
            background: #10b981;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 14px;
            margin-bottom: 20px;
        }
        
        .print-button:hover {
            background: #059669;
        }
    </style>
</head>
<body>
    <button class="print-button" onclick="window.print()">🖨️ Imprimer / Télécharger en PDF</button>
    
    <div class="container">
        <div class="header">
            <h1>📋 Contrat de Sponsoring</h1>
            <p>Accord commercial entre FC Club et le sponsor</p>
        </div>
        
        <div class="content">
            <!-- Sponsor Information -->
            <div class="section">
                <div class="section-title">Sponsor</div>
                <div class="info-group">
                    <div class="info-item">
                        <div class="info-label">Nom du sponsor</div>
                        <div class="info-value">{{ contrat_sponsor.sponsor.nom }}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Email</div>
                        <div class="info-value">{{ contrat_sponsor.sponsor.email }}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Téléphone</div>
                        <div class="info-value">{{ contrat_sponsor.sponsor.telephone }}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Budget annuel</div>
                        <div class="info-value">{{ contrat_sponsor.sponsor.budget }}€</div>
                    </div>
                </div>
            </div>
            
            <!-- Contract Details -->
            <div class="section">
                <div class="section-title">Détails du Contrat</div>
                <div class="info-group">
                    <div class="info-item">
                        <div class="info-label">Date de début</div>
                        <div class="info-value">{{ contrat_sponsor.dateDebut ? contrat_sponsor.dateDebut|date('d/m/Y') : 'Non définie' }}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Date de fin</div>
                        <div class="info-value">{{ contrat_sponsor.dateFin ? contrat_sponsor.dateFin|date('d/m/Y') : 'Non définie' }}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Montant du contrat</div>
                        <div class="info-value">{{ contrat_sponsor.montant }}€</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Équipe partenaire</div>
                        <div class="info-value">{{ contrat_sponsor.equipe.nom ?? 'Non assignée' }}</div>
                    </div>
                </div>
            </div>
            
            <!-- Duration Calculation -->
            <div class="section">
                <div class="section-title">Durée et Conditions</div>
                {% if contrat_sponsor.dateDebut and contrat_sponsor.dateFin %}
                    {% set interval = contrat_sponsor.dateFin.diff(contrat_sponsor.dateDebut) %}
                    <div class="info-group">
                        <div class="info-item">
                            <div class="info-label">Durée du contrat</div>
                            <div class="info-value">{{ interval.days }} jours</div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Montant par jour</div>
                            <div class="info-value">{{ (contrat_sponsor.montant / (interval.days + 1))|round(2) }}€</div>
                        </div>
                    </div>
                {% endif %}
                <div class="info-group full-width">
                    <div class="info-item">
                        <div class="info-label">Conditions générales</div>
                        <div class="info-value">
                            Accord de partenariat entre FC Club et {{ contrat_sponsor.sponsor.nom }} pour une périodecommençant le {{ contrat_sponsor.dateDebut ? contrat_sponsor.dateDebut|date('d/m/Y') : 'TBD' }} et se terminant le {{ contrat_sponsor.dateFin ? contrat_sponsor.dateFin|date('d/m/Y') : 'TBD' }}.
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Contract Description -->
            {% if contrat_sponsor.description %}
            <div class="section">
                <div class="section-title">Description et Contraintes du Contrat</div>
                <div class="info-group full-width">
                    <div class="info-item">
                        <div class="info-value" style="white-space: pre-wrap; line-height: 1.8;">{{ contrat_sponsor.description }}</div>
                    </div>
                </div>
            </div>
            {% endif %}
        
        <!-- Signatures -->
        <div class="signature-section">
            <div class="signature-box">
                <h4>FC Club</h4>
                <div class="signature-line">
                    Signature et Tampon
                </div>
            </div>
            <div class="signature-box">
                <h4>{{ contrat_sponsor.sponsor.nom }}</h4>
                <div class="signature-line">
                    Signature et Tampon
                </div>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p>Document généré le {{ 'now'|date('d/m/Y à H:i') }}</p>
            <p>Contrat n° {{ contrat_sponsor.id }} - FC Club Administration</p>
        </div>
    </div>
</body>
</html>
