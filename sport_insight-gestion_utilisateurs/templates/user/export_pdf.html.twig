<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            color: #333;
            background: #fff;
            padding: 20px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 3px solid #0066cc;
            padding-bottom: 15px;
        }
        
        .header h1 {
            font-size: 28px;
            color: #0066cc;
            margin-bottom: 10px;
        }
        
        .header p {
            color: #666;
            font-size: 12px;
        }
        
        .stats-container {
            display: flex;
            justify-content: space-around;
            margin-bottom: 25px;
            background: #f5f5f5;
            padding: 15px;
            border-radius: 5px;
        }
        
        .stat-box {
            text-align: center;
        }
        
        .stat-number {
            font-size: 24px;
            font-weight: bold;
            color: #0066cc;
        }
        
        .stat-label {
            font-size: 12px;
            color: #666;
            margin-top: 5px;
        }
        
        .info {
            text-align: right;
            font-size: 11px;
            color: #999;
            margin-bottom: 15px;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }
        
        table th {
            background: #0066cc;
            color: white;
            padding: 12px;
            text-align: left;
            font-size: 12px;
            font-weight: bold;
        }
        
        table td {
            padding: 10px 12px;
            border-bottom: 1px solid #ddd;
            font-size: 11px;
        }
        
        table tr:nth-child(even) {
            background: #f9f9f9;
        }
        
        table tr:hover {
            background: #f0f0f0;
        }
        
        .status-active {
            background: #d4edda;
            color: #155724;
            padding: 4px 8px;
            border-radius: 3px;
            font-weight: bold;
        }
        
        .status-blocked {
            background: #f8d7da;
            color: #721c24;
            padding: 4px 8px;
            border-radius: 3px;
            font-weight: bold;
        }
        
        .role-badge {
            background: #e2e3e5;
            color: #383d41;
            padding: 3px 6px;
            border-radius: 3px;
            margin-right: 4px;
            display: inline-block;
            font-size: 10px;
        }
        
        .footer {
            margin-top: 30px;
            padding-top: 15px;
            border-top: 1px solid #ddd;
            text-align: center;
            font-size: 10px;
            color: #999;
        }
        
        .page-break {
            page-break-after: always;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🏅 Liste des Utilisateurs</h1>
        <p>Sport Insight - Rapport de Gestion</p>
    </div>
    
    <div class="info">
        <strong>Date d'export:</strong> {{ exportDate|date('d/m/Y à H:i:s') }}<br>
        <strong>Nombre d'enregistrements:</strong> {{ filteredCount }}
    </div>
    
    <div class="stats-container">
        <div class="stat-box">
            <div class="stat-number">{{ filteredCount }}</div>
            <div class="stat-label">Total Utilisateurs</div>
        </div>
        <div class="stat-box">
            <div class="stat-number">{{ users|length }}</div>
            <div class="stat-label">Utilisateurs Affichés</div>
        </div>
    </div>
    
    <table>
        <thead>
            <tr>
                <th style="width: 5%;">#</th>
                <th style="width: 20%;">Nom Complet</th>
                <th style="width: 25%;">Email</th>
                <th style="width: 15%;">Téléphone</th>
                <th style="width: 15%;">Statut</th>
                <th style="width: 20%;">Rôles</th>
                <th style="width: 15%;">Inscription</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td><strong>{{ user.prenom }} {{ user.nom }}</strong></td>
                <td>{{ user.email }}</td>
                <td>{{ user.telephone ?? '---' }}</td>
                <td>
                    {% if user.isActif %}
                        <span class="status-active">Actif</span>
                    {% else %}
                        <span class="status-blocked">Bloqué</span>
                    {% endif %}
                </td>
                <td>
                    {% for role in user.roles %}
                        <span class="role-badge">{{ role }}</span>
                    {% endfor %}
                </td>
                <td>{{ user.dateInscription|date('d/m/Y') }}</td>
            </tr>
            {% else %}
            <tr>
                <td colspan="7" style="text-align: center; padding: 20px;">
                    Aucun utilisateur trouvé
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="footer">
        <p>&copy; 2026 Sport Insight - Système de Gestion Utilisateurs</p>
        <p>Ce rapport a été généré automatiquement le {{ exportDate|date('d/m/Y à H:i:s') }}</p>
    </div>
</body>
</html>
