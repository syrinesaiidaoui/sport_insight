{% extends 'back_office/base.html.twig' %}

{% block title %}Nouveau contrat sponsor{% endblock %}

{% block body %}
    <div class="main-content">
        <div class="page-header">
            <h1>Nouveau contrat sponsor</h1>
        </div>

        <div class="card form-card">
            {{ include('contrat_sponsor/_form.html.twig') }}
        </div>

        <div class="mt-3">
            <a href="{{ path('app_contrat_sponsor_index') }}" class="btn btn-secondary">
                <i class="lucide-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>

    <style>
        .page-header {
            margin-bottom: 30px;
        }
        .page-header h1 {
            margin: 0;
            color: var(--text-dark);
        }
        .form-card {
            background: var(--card-bg);
            border-radius: 8px;
            padding: 30px;
            border: 1px solid var(--border-color);
            max-width: 600px;
        }
        .btn-secondary {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            background: var(--text-muted);
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            text-decoration: none;
            transition: background 0.3s;
        }
        .btn-secondary:hover {
            background: #4b5563;
            color: white;
        }
        .mt-3 {
            margin-top: 20px;
        }
    </style>
{% endblock %}
